# v0.82 Prepositions Completion Pass QA

## 1. Starting Version Verified

Starting repo state contained `index.html`, `content/*.js`, `content/b1-cases-articles.js`, `data/article-items.tsv`, v0.81.1 docs, current strategic docs, and AGENTS pointers. Runtime metadata before the patch was `v0.81 - Cases and Articles Completion Pass` / `v0.81-cases-articles-completion`.

## 2. Strategic Docs / Feature Register Check

`docs/OZMOSIS_FEATURE_REGISTER_CURRENT.md` marked `prepositions_completion` as `allowed_now: yes` and `word_order_completion` as `allowed_now: no`.

## 3. Files Inspected

Inspected `index.html`, `AGENTS.md`, `content/*.js`, `data/article-items.tsv`, v0.81/v0.81.1 README/changelog/source brief/roadmap files, current strategic docs, existing QA reports, and the attached v0.82 prompt.

## 4. Files Changed

Changed `index.html`, `content/b1-prepositions.js`, `AGENTS.md`, `CHANGELOG-FULL-OZMOSIS.txt`, current strategic docs, and v0.82 versioned docs/QA.

## 5. Coverage Audit Method

Existing explicit preposition coverage was counted only where items had explicit tags/targets or bank metadata pointing at prepositions. Incidental preposition words in unrelated prompts were not counted.

## 6. Existing Preposition Coverage

Existing explicit coverage before v0.82: 58 items, mostly from B1-15 time/place/movement. Task depths: 10 contrast, 36 cloze, 12 repair. Main gaps: no dedicated route, limited fixed accusative coverage, limited structured variant families, and sparse review metadata.

## 7. New Items Added

Added 92 original items:

- recognition: 22
- cloze: 24
- repair: 22
- contrast: 24

Coverage by class:

- fixed dative: 28
- fixed accusative: 20
- two-way: 36
- temporal: 8

## 8. Metadata Repairs

All new items include `targetId`, `variantFamilyId`, `reviewGroup`, `taskDepth`, `itemType`, `contentBankKey`, `moduleKey`, `prepositionTargets`, `prepositionClasses`, and `caseTargets`.

## 9. Review Coverage Impact

Prepositions are eligible for the existing conservative review metadata path. They are excluded from B1 Path star gating by `b1PathEligible:false`.

## 10. Skill Matrix Impact

New evidence maps to the existing `prepositions` Learning Map / Skill Matrix node through explicit skill tags and bank key.

## 11. Learning Map Impact

The existing prepositions node remains conservative. No mastery, readiness, or percentage-based completion claim was added.

## 12. Suggested Next Practice Impact

Prepositions recommendations now route to the new Prepositions route instead of falling back to Today's Sprint.

## 13. Content Bank Impact

Added/updated only `content/b1-prepositions.js`. Other B1 content banks were not changed.

## 14. Evidence/Scoring Impact

Evidence metadata is additive. Scoring, correctness, confidence values, star logic, Star Threshold Overlay semantics, production semantics, and diagnostic semantics were not changed.

## 15. localStorage Impact

Protected keys remain present and unchanged:

- `ozmosis_b1_sprint_progress_v1`
- `ozmosis_b1_progress_evidence_v1`
- `ozmosis_b1_path_unlock_overrides_v1`
- `ozmosis_b1_review_edit_queue_v1`
- `ozmosis_b1_progress_summary_v1`

## 16. Export/Import Impact

`Export Save`, `Import Save`, import file input, and `validateOzmosisSavePayload` remain present in source. File-picker export/import was not completed because browser route validation was blocked.

## 17. Route Isolation Impact

Route wiring uses the existing imported-module route path. Static route gate confirms the menu button, Grammar hub button, start function, recommendation route, and debug route helper exist. Browser route isolation could not be completed in this environment.

## 18. Route Smoke

Static route smoke passed. Browser route smoke was attempted through Edge/CDP and the in-app Browser path, but did not complete.

## 19. Responsive Results

No responsive browser result is claimed. The Edge/CDP gate was blocked before viewport checks completed.

## 20. Arabic/LTR Results

No browser Arabic/LTR result is claimed. Source preserves `dir="ltr"` practice input conventions and no Arabic-specific route changes were made.

## 21. Tests Run

- Parsed `index.html` inline script.
- Parsed all `content/*.js`.
- Validated the new prepositions bank count, metadata, duplicate IDs, repair no-op status, and Daily/Diagnostic exclusion.
- Ran static route/source gate for script inclusion, menu/Grammar hub buttons, registry flags, recommendation route, debug count, storage keys, Export/Import strings, import validation, B1-18 preview-only status, and feature-register state.
- Attempted Edge/CDP browser gate with local static server.
- Attempted in-app Browser setup.

## 22. Tests Not Run and Why

- Browser route rendering, responsive viewport smoke, Arabic browser smoke, real save export/import, and route isolation interaction were not completed. Edge/CDP timed out waiting for the loaded app marker. The in-app Browser path failed with the Windows sandbox error `CreateProcessAsUserW failed: 5`.
- Live GitHub Pages was not checked.

## 23. Known Risks

- The Prepositions route needs manual/browser acceptance before Word Order starts.
- Browser performance/load timing should be checked separately because this environment could not observe the loaded app marker.
- Existing B1-01 metadata gaps remain non-blocking but unresolved.

## 24. Recommended Next Patch

`v0.82.1 - Prepositions Browser Acceptance Gate`
