# Ozmosis v0.51 Deployment / Device QA Gate

## 1. Starting Version Verified

Repo-root `index.html` verified before v0.52 work:

- `OZMOSIS_APP_VERSION = "v0.51"`
- `OZMOSIS_EXPORT_VERSION = "v0.51-skill-matrix-v1"`
- `B1_STORAGE_KEY = "ozmosis_b1_sprint_progress_v1"`
- `B1_EVIDENCE_STORAGE_KEY = "ozmosis_b1_progress_evidence_v1"`

`README.md` and `AGENTS.md` verified:

- Current working version: `v0.51`
- Next planned patch: `v0.52 - Support-Level Tracking`

`CHANGELOG.txt` latest entry verified as `v0.51 - Skill Matrix v1`.

## 2. Repo Root Verified

The active terminal directory was the parent folder, so the expected repo root was checked explicitly:

`C:\Users\rossm\Documents\German Learning\ozmosis-repo`

Required repo-root files and folders were present:

- `index.html`
- `README.md`
- `CHANGELOG.txt`
- `AGENTS.md`
- `content/`
- `docs/`

## 3. Browser Validation Method

Browser validation used Microsoft Edge headless via Chrome DevTools Protocol against a local static HTTP server.

The Edge launch required running outside the filesystem sandbox because browser processes cannot expose the CDP port reliably from inside the sandbox. The method used real DOM clicks/state, captured console errors, captured runtime exceptions, and checked rendered layout dimensions.

The in-app Browser plugin and bundled Playwright path were not claimed as working.

## 4. Local Browser Result

Passed:

- Home app loaded.
- Learning Map opened by DOM click.
- Task-depth matrix rendered with title `Task-depth matrix`.
- Dev Tools opened by DOM click.
- `window.ozmosisB1Debug` existed.
- `taskDepthSchemaVersion` was `v1`.
- Skill Matrix data existed.
- Console error count: `0`.
- Runtime error count: `0`.

Responsive smoke was run at:

- `320x568`
- `390x844`
- `430x932`
- `768x1024`
- `1365x599`
- `1440x900`

The matrix was reachable at all required viewports. Mobile widths used intentional internal matrix scrolling. No body-level horizontal overflow was detected. Some controls were naturally below the fold on small viewports and remained scroll-reachable.

## 5. Live Deployment Check, if run

Not run. Network/deployment verification was outside the local gated coding path for this run.

## 6. Manual Device Checks Still Needed

Manual real-device checks were not run.

Recommended manual checks:

- Real phone touch scrolling on the Learning Map matrix.
- Real phone keyboard flow through active practice.
- Live GitHub Pages deployment freshness after an intentional deployment.

## 7. Documentation Filename Hygiene Findings

The current canonical source brief and roadmap content reflects v0.51, but the filenames are stale:

- `docs/OZMOSIS_SOURCE_BRIEF_v0.48.md`
- `docs/OZMOSIS_DEVELOPMENT_ROADMAP_v0.48.md`

This does not block v0.52 implementation because the file contents clearly identify the current development position and next patch. Filename hygiene is deferred to the final documentation hygiene stage.

## 8. Files Changed

- `docs/qa/v0.51_deployment_device_qa_gate.md`

No app behavior was changed for this gate note.

## 9. Tests Run

- Repo-root marker verification.
- Version and storage-key verification.
- README/AGENTS current/next version verification.
- CHANGELOG latest-entry verification.
- Source brief / roadmap current-position verification.
- QA report existence checks.
- Inline `index.html` script syntax parse.
- Content script syntax parse:
  - `content/b1-connectors.js`
  - `content/b1-redemittel.js`
  - `content/b1-wortschatz.js`
- Edge/CDP local browser smoke.
- Edge/CDP responsive smoke at the required viewport matrix.

## 10. Tests Not Run and Why

- In-app Browser plugin: not run because earlier local runtime setup failures remain known and Edge/CDP is the approved path for this environment.
- Bundled Playwright: not run because earlier local runs found the bundled Playwright path unavailable.
- Live deployment check: not run because local package verification was the gating requirement for implementation.
- Manual device checks: not run because no human device was available in this coding environment.

## 11. Recommended Next Patch

Proceed to `v0.52 - Support-Level Tracking`.
