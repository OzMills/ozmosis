# v0.85.11-live Manual Deployment / URL Steps

## Current Fact

`https://ozmills.github.io/ozmosis/` serves Ozmosis v0.85.11.

`https://ozmills.github.io/kasuskonsole/` returns HTTP 404.

## If /ozmosis/ Is The Correct Canonical URL

1. Treat `https://ozmills.github.io/ozmosis/` as the live app URL.
2. Update any bookmarks, README references, shared links, and external notes that still point to `/kasuskonsole/`.
3. Re-run a quick live check for `https://ozmills.github.io/ozmosis/`.
4. Once the project owner confirms `/ozmosis/` is canonical, v0.86 may be unblocked by policy.

## If /kasuskonsole/ Must Remain The Public URL

1. Open GitHub and verify whether a repository named `OzMills/kasuskonsole` exists.
2. If it exists, configure its GitHub Pages source and publish the current v0.85.11 `index.html`, `content/`, `data/`, `manifest.json`, and required assets/docs there.
3. If it does not exist, create or restore the repo only if that is the intended project URL.
4. Alternatively, rename/reconfigure the current Pages project only after deciding that the repo-name URL should change.
5. Re-check `https://ozmills.github.io/kasuskonsole/` and confirm it contains `v0.85.11` and `v0.85.11-final-regression-live-readiness`.

## Do Not

- Do not change app runtime code for this path issue.
- Do not start v0.86 until the canonical live URL decision is made.
- Do not push from this Codex environment; `git` is unavailable and the Pages settings are not inspectable here.
