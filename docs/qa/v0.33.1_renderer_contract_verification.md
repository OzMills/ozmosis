# Ozmosis QA Verification Report

## Patch under review

Version under review: `v0.33.1`
Patch name: `B1 Renderer Contract and Feedback Cleanup`
Date tested: `2026-05-21`
Tester: `Codex`
Source of truth: `repo-root index.html`

## Scope

This report verifies whether the current repo actually implements the requested renderer-contract cleanup.

This is a verification pass only.

No feature work, content expansion, renderer rewrites, storage migrations, or UI redesigns were performed.

---

## 1. Repository state

| Check | Result | Evidence / Notes |
|---|---:|---|
| Current version is v0.33.1 | PASS | `index.html` export metadata contains `v0.33.1-renderer-contract-cleanup`; `CHANGELOG.txt:1` is `## v0.33.1 - B1 Renderer Contract and Feedback Cleanup`; `README.md:21` says current working version is `v0.33.1`. |
| `index.html` is repo-root source of truth | PASS | Verification used `C:\Users\rossm\Documents\German Learning\ozmosis-repo\index.html`. Root file size inspected: 536,574 bytes. |
| `CHANGELOG.txt` includes v0.33.1 | PASS | `CHANGELOG.txt:1` contains the v0.33.1 entry. |
| `README.md` / `AGENTS.md` do not contradict current version | PASS | `README.md:21` and `AGENTS.md:9` both identify current working version as `v0.33.1`; both list next planned patch as `v0.34`. |
| No duplicate loose `index.html` used as source of truth | PASS | Root file scan found only repo-root `index.html` matching `index*.html`; no loose standalone index was used. |
| No committed `test-results/`, screenshots, ZIPs, temp files | BLOCKED | Git metadata/`git` was not available in this workspace, so committed state could not be verified. Local ignored artefacts exist: `test-results/`, `repo_contents.zip`, and `__chrome_profile_v0321`; `.gitignore` excludes `test-results/`, `*.zip`, `screenshots/`, `playwright-report/`, and `__chrome_profile*/`. |

---

## 2. Known issue verification table

| Issue | Expected behaviour | Result | Evidence | Remaining problem |
|---|---|---:|---|---|
| Choice items render fake blanks | Choice prompts show text + buttons only; no fake `___` blank | PASS | Runtime: Redemittel `b1_redemittel_071` rendered prompt `Formulierung zur Versicherung:` with three choice buttons and `hasInline:false`, `blankSlots:0`. Connector `b1_connector_002` rendered choices with no editable input. Static scan found `badChoice: []` for Redemittel phrase-choice prompts. | None found. |
| Prompt renderer appends blanks too broadly | Only typed cloze item types render blanks | PASS | Inspection: `b1PromptHtml` at `index.html:12941`, `b1IsTypedCloze` at `index.html:12922`, `b1UsesChoices` at `index.html:12919`, and `b1PromptWithoutFakeBlank` at `index.html:12926`. Runtime: choice and production items had `hasInline:false`, `blankSlots:0`; typed cloze had `hasInline:true` before submit. | None found. |
| Correct choice feedback repeats answer | Correct choice highlights button only; no duplicated answer card | PASS | Runtime: Redemittel `b1_redemittel_071` correct feedback was only `✓` plus star display; correct choice button had class `correct`. Connector `b1_connector_002` behaved the same. No `Correct answer:` appeared on correct choice submissions. | None found. |
| Correct typed-cloze feedback repeats answer/sentence | Correct typed cloze shows green filled cloze only; no `Correct answer: X` | PASS | Runtime: `b1_redemittel_055` correct submit changed prompt to `Ich würde gern bei Ihnen arbeiten.` with one `.clozeAnswerText.correct`, no editable input, and feedback `✓` plus stars only. No full-sentence duplicate feedback and no `Correct answer:` line. | None found. |
| Wrong typed-cloze correction | Wrong typed cloze shows red corrected cloze in-place | PASS | Runtime: `b1_redemittel_064` wrong submit changed prompt to `Ich bin krank und kann heute nicht kommen.` with one `.clozeAnswerText.missed`; feedback showed concise `Correct answer: kommen` plus one usage note. | None found. |
| Error-repair feedback duplicates corrected answer | Correct repair does not repeat corrected sentence unnecessarily | PASS | Runtime: `b1_redemittel_025` correct repair changed prompt to `Bessere Form: Ich möchte gern einen Termin vereinbaren.` and feedback was only `✓` plus stars. No repeated corrected sentence in feedback. | None found. |
| Production-card feedback feels graded | Mark-practised feels self-marked, not scored/graded | PASS | Runtime: `b1_redemittel_084` showed `Show model` and `Mark practised`; after mark-practised feedback was `Practised` and `Saved to local practice progress.` No correct/wrong correction card appeared. |
| English `prompt_de` labels in Redemittel | Redemittel `prompt_de` labels are German | PASS | Static Redemittel prompt scan found no phrase-choice prompts matching the known English labels (`Best`, `Polite`, `Formal`, `Insurance`, `Callback`, `Most`). Runtime observed German labels: `Formulierung zur Versicherung:`, `Formelle Anrede:`, and `Bessere Form:`. | A full linguistic proofread of all Redemittel content was outside this verification scope. |
| Redemittel content preserved | Existing v0.33 Redemittel content remains present | PASS | Static inspection counted 90 Redemittel items with expected distribution: formal email 15, appointment 12, request 12, complaint 12, application 12, illness/absence 10, polite requests 10, production 7. | None found. |
| Seeded randomisation preserved | B1 randomisation and recent-start logic still work | PASS | Inspection: `buildB1Round` at `index.html:12818`; recent-start logic around `index.html:12326`, `12329`, `12337`, `12772`, `12781`, `12782`, `12785`. Runtime repeated-start smoke: Connector first IDs were `b1_connector_003`, `016`, `008`, `020`, `017`; Mixed Redemittel first IDs were `b1_redemittel_052`, `077`, `064`, `073`, `024`. Recent starts persisted after reload. | Statistical guarantee was not exhaustively tested; smoke result passed. |
| Connector word-order fix preserved | No `denn ich krank bin`; `weil/denn/deshalb` word order still consistent | PASS | Runtime content audit returned no issues. Specific audit check found no prompt equivalent to `___ ich krank bin` accepting/revealing `denn`. Connector tests included `b1_connector_002` (`weil` + verb-final) and `b1_connector_018` (`deshalb` + inversion). | Only automated heuristic QA was run, not a full expert linguistic review of all connector items. |
| Mobile typed-cloze submit visibility preserved | `Prüfen` remains visible in focused mobile cloze mode | PASS | Runtime at 393×852: `body` class included `mobileClozeFocus`; `#checkBtn` visible with rect top 313, bottom 369 inside viewport height 852; English support visible. Responsive DOM checks passed at 320×568, 390×844, 393×852, and 430×932. | Real iOS Safari keyboard/accessory-bar test was blocked/not run. |

---

## 3. Function/data inspection

| Area inspected | Function / data | Result | Notes |
|---|---|---:|---|
| Prompt rendering | `b1PromptHtml` | PASS | Located at `index.html:12941`; runtime showed typed cloze receives inline input before submit while choices/production do not. |
| Typed-cloze detection | `b1IsTypedCloze` | PASS | Located at `index.html:12922`; runtime typed cloze state applied to Redemittel cloze items only in tested flow. |
| Choice detection | `b1UsesChoices` | PASS | Located at `index.html:12919`; connector and Redemittel choice items rendered choice buttons without editable blanks. |
| Fake blank stripping | `b1PromptWithoutFakeBlank` | PASS | Located at `index.html:12926`; runtime phrase-choice prompts displayed German labels without trailing fake blanks. |
| Feedback rendering | `renderB1Feedback` | PASS | Located at `index.html:13294`; correct cloze/choice/repair feedback was minimal; wrong cloze/choice retained concise correction. |
| Answer checking | `checkB1Answer` | PASS | Located at `index.html:13331`; typed and choice correct/wrong paths worked in runtime tests. |
| Production-card practised state | `markB1ProductionPractised` | PASS | Located at `index.html:13313`; runtime production-card flow saved a practised state without graded correction feedback. |
| Redemittel bank | `b1_redemittel_*` items | PASS | 90 items present; expected category counts preserved; phrase-choice German-label scan passed. |
| Connector bank | `b1_connector_*` items | PASS | Connector smoke and choice tests passed; content audit returned no `weil`/`denn`/`deshalb` regression findings. |
| Randomisation | seed/shuffle/recent-start helpers | PASS | `buildB1Round` and recent-start storage inspected; repeated starts varied and recent-start metadata persisted after reload. |

---

## 4. Runtime test results

| Test | Viewport | Result | Evidence / Notes |
|---|---:|---:|---|
| App loads with no console errors | default | PASS | Playwright/Chrome load showed title `Ozmosis`, landing page visible, and no collected console/page errors. |
| Redemittel hub opens | default | PASS | Redemittel navigation reached the hub. |
| Mixed Redemittel starts | default | PASS | Mixed Redemittel started with sampled item `b1_redemittel_030`. |
| Redemittel phrase_choice before submit: no fake blank | 393×852 | PASS | `b1_redemittel_071`: `Formulierung zur Versicherung:` plus choices; `hasInline:false`, `blankSlots:0`. |
| Redemittel phrase_choice correct: no duplicated answer card | 393×852 | PASS | `b1_redemittel_071`: correct choice class applied; feedback only `✓` plus stars. |
| Redemittel phrase_choice wrong: concise correction | 393×852 | PASS | `b1_redemittel_009`: wrong choice class `missed`, correct class `correct`, feedback `Correct answer: Sehr geehrte Frau Müller,` plus one short note. |
| Typed cloze correct: green filled cloze only | 393×852 | PASS | `b1_redemittel_055`: editable input replaced by one `.clozeAnswerText.correct`; no `Correct answer:` line. |
| Typed cloze wrong: red corrected cloze | 393×852 | PASS | `b1_redemittel_064`: editable input replaced by one `.clozeAnswerText.missed`; concise correction shown. |
| Error repair correct: no duplicated corrected phrase | 393×852 | PASS | `b1_redemittel_025`: repaired phrase inserted in prompt; feedback did not repeat the phrase. |
| Production card mark-practised: self-marked feel | 393×852 | PASS | `b1_redemittel_084`: after marking, feedback was `Practised` and `Saved to local practice progress.` |
| Connector Sprint still starts | default | PASS | Connector Sprint started; sampled item `b1_connector_021`. |
| Case Trainer still starts | default | PASS | Case Trainer smoke reached the existing practice flow without runtime errors. |
| Options opens/closes | default | PASS | Options smoke succeeded. |
| Dev Tools opens/closes | default | PASS | Dev Tools smoke succeeded. |
| Progress persists after reload | default | PASS | After answering an item and reloading, progress remained under `ozmosis_b1_sprint_progress_v1`. |
| Randomisation: repeated starts vary | default | PASS | Connector repeated starts produced 5 unique first IDs; Mixed Redemittel repeated starts produced 5 unique first IDs. |
| Arabic UI keeps German content LTR | 393×852 | PASS | Arabic smoke reported German prompt `dir:ltr` and input `dir:ltr`. |

---

## 5. Responsive checks

| Viewport | Result | Notes |
|---:|---:|---|
| 320×568 | PASS | DOM overflow checks reported `homeOverflow:0`, `activeOverflow:0`; focused typed cloze had `mobileClozeFocus` and visible `Prüfen`. |
| 390×844 | PASS | DOM overflow checks reported `homeOverflow:0`, `activeOverflow:0`; focused typed cloze had visible `Prüfen`. |
| 393×852 iPhone 15 Pro portrait | PASS | DOM overflow checks reported `homeOverflow:0`, `activeOverflow:0`; focused typed cloze had visible `Prüfen`; real iOS keyboard was not tested. |
| 430×932 | PASS | DOM overflow checks reported `homeOverflow:0`, `activeOverflow:0`; focused typed cloze had visible `Prüfen`. |
| 768×1024 | PASS | DOM overflow checks reported `homeOverflow:0`, `activeOverflow:0`; `Prüfen` visible. |
| 1365×599 | PASS | DOM overflow checks reported `homeOverflow:0`, `activeOverflow:0`; `Prüfen` visible. |
| 1440×900 | PASS | DOM overflow checks reported `homeOverflow:0`, `activeOverflow:0`; `Prüfen` visible. |

---

## 6. Manual phone retest checklist for Oz

- [ ] Redemittel phrase_choice before submit: no fake blank.
- [ ] Redemittel phrase_choice correct: selected answer green, no duplicated answer card.
- [ ] Redemittel phrase_choice wrong: wrong/correct choices clear, concise correction.
- [ ] Typed cloze correct: input becomes green filled cloze text, no `Correct answer: X`.
- [ ] Typed cloze wrong: input becomes red corrected cloze text.
- [ ] Error repair correct: no repeated corrected sentence.
- [ ] Production card: mark-practised feels self-marked, not graded.
- [ ] `Prüfen` visible with mobile keyboard open.
- [ ] No `denn ich krank bin`.
- [ ] Progress persists after reload.

---

## 7. Tests not run / blocked

| Test | Status | Reason |
|---|---:|---|
| Real iOS Safari keyboard test | BLOCKED | The environment can emulate mobile viewport/focused input in Chromium, but it cannot run real iOS Safari or the iOS keyboard/accessory bar. |
| Screenshot capture | NOT RUN | No screenshots were requested as deliverables for this verification file, and the prompt required avoiding new test artefacts. |
| LocalStorage persistence | RUN | Verified by answering an item, reloading, and confirming `ozmosis_b1_sprint_progress_v1` retained progress/recent-start metadata. |
| Arabic RTL/LTR smoke | RUN | Verified German prompt and input remained `dir:ltr` in Arabic UI smoke. |
| Responsive visual inspection | RUN | Performed via Playwright viewport/DOM overflow and visibility checks at required sizes. No screenshot-based visual inspection was performed. |

---

## 8. Final verdict

Overall result: `PASS`

Summary:

- v0.33.1 version markers are present in `index.html`, `CHANGELOG.txt`, `README.md`, and `AGENTS.md`.
- Renderer contract checks passed for typed cloze, choice, error repair, and production-card flows.
- Choice items no longer showed fake editable blanks in tested Connector and Redemittel flows.
- Correct typed-cloze and choice feedback no longer duplicated the correct answer in tested flows.
- Wrong typed-cloze correction appeared in-place with red corrected cloze text.
- Redemittel v0.33 content appears preserved at 90 items with the expected category distribution.
- Seeded randomisation and recent-start anti-repeat passed smoke testing.
- Blocked coverage remains: real iOS Safari keyboard testing and committed-artefact verification.

Recommended next step:

Run a real-device iPhone Safari gate for mobile typed-cloze keyboard behaviour. If that passes, proceed to the next planned content patch, `v0.34`, rather than opening a v0.33.2 fix.
