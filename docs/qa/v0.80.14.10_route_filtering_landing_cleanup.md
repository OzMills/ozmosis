# Ozmosis v0.80.14.10 QA - Learner Route Filtering and Landing Hub Cleanup

## 1. Starting Version Verified

Verified before editing:

- `index.html`
- `AGENTS.md`
- `content/`
- `docs/`
- `README-v0_80_14_9.md`
- `CHANGELOG-v0_80_14_9.txt`

Starting versions:

- `OZMOSIS_APP_VERSION = "v0.80.14.9"`
- `OZMOSIS_EXPORT_VERSION = "v0.80.14.9-full-b1-v02-content-import"`

Protected keys were present:

- `ozmosis_b1_sprint_progress_v1`
- `ozmosis_b1_progress_evidence_v1`

Additive compatible keys were present:

- `ozmosis_b1_path_unlock_overrides_v1`
- `ozmosis_b1_review_edit_queue_v1`

## 2. First Inspection Result

- Current app version: `v0.80.14.9`
- Current export version: `v0.80.14.9-full-b1-v02-content-import`
- Repo-root `index.html` exists.
- Content files are present.
- CAT01-CAT18 content files are present.
- Imported row metadata is stored on generated item objects in `content/b1-m*.js`.
- Source/review metadata fields include `sourceFile`, `sourceTaskCsv`, `sourceRowId`, `sourceRowNumber`, `sourceRowAuditKey`, `originalStage3Decision`, `reviewStatus`, `originalReviewStatus`, `needsHumanReview`, `reviewFlag`, `reviewFlagReason`, `humanReviewReason`, `issueCodes`, `activeB1Evidence`, and `previewOnly`.
- Source family can be derived from `sourceFile` / `sourceTaskCsv`.
- Normal learner routes select through `b1ItemIsNormalEligible`, `buildB1Round`, `b1PoolFor`, route pool helpers, and imported module pool helpers.
- Today’s B1 Sprint selects through `b1RegistryDailyEligibleItems`, `b1PoolFor("mixed")`, and persisted daily IDs.
- Review Practice selects through `buildB1ReviewCandidatePreview` and `b1ReviewPracticeItems`.
- B1 Path/star/progress summaries select through B1 Path progress helpers and evidence summaries.
- Diagnostic Sprint selects through `b1DiagnosticEligibleItems` and `b1DiagnosticBuildQueue`.
- Dev Tools Review Tests select through `b1ReviewFlaggedImportedItems` and `startB1ReviewTests`.
- Review Edit List writes to `ozmosis_b1_review_edit_queue_v1`.
- CAT18 preview-only gating uses `previewOnly`, `categoryId === "CAT18"`, and `activeB1Evidence:false`.
- Home/landing cards are rendered in the `homeScreen` block and `renderHomeCommandScreen`.
- Validation gates live in `_development-files/`.

No source-inspection blocker was found.

## 3. Files Changed

- `index.html`
- `AGENTS.md`
- `content/b1-m01-variant-families.js`
- `content/b1-m03-job-search-offices.js`
- `content/b1-m08-orders-delivery.js`
- `_development-files/v0801410_route_filtering_landing_cleanup_gate.cjs`
- `_development-files/v0801410_route_filtering_landing_cleanup_browser_smoke.cjs`
- `README-v0_80_14_10.md`
- `CHANGELOG-v0_80_14_10.txt`
- `docs/OZMOSIS_SOURCE_BRIEF_v0.80.14.10.md`
- `docs/OZMOSIS_DEVELOPMENT_ROADMAP_v0.80.14.10.md`
- `docs/qa/v0.80.14.10_route_filtering_landing_cleanup.md`

## 4. Files Intentionally Not Changed

- Existing source bundles and CSVs.
- Broad content banks outside the five source-backed visible-choice corrections.
- Existing Review Tests and Review Edit List workflows.
- Protected storage keys.
- Scoring, correctness rules, confidence values, production self-marking, Today’s B1 Sprint structure, CAT18 preview source material, and B1-only active scope.

## 5. Route Filtering Implementation

Added central helpers for:

- normal learner practice eligibility
- Review Practice eligibility
- B1 Path/star eligibility
- Dev Tools Review Tests eligibility
- learner-progress record filtering
- route-filtering debug summary

Normal imported learner routes now require:

- imported source family is `PRACTICE_FAMILIES` or `CONTRAST_SETS`
- no `needsHumanReview`
- no `reviewFlag`
- not `move_to_human_review`
- not CAT18 / preview / inactive B1 evidence
- no generated prompt/choice artefact

## 6. Human-Review Row Policy

Human-review rows are excluded from:

- normal learner routes
- Today’s B1 Sprint
- Review Practice
- B1 Path stars/progress
- normal B1 progress/evidence summaries
- learner-facing recommendations

They remain available for:

- Dev Tools Review Tests
- Review Edit List
- debug/source metadata
- export/import metadata

## 7. Bank-Generated Row Policy

Generic imported `WORTSCHATZ_BANK`, `COLLOCATION_BANK`, and `REDEMITTEL_BANK` rows are excluded from normal learner routes and preserved for Review Tests / QA.

The v0.80.14.10 gate confirms no bank-generated imported rows are normal learner eligible.

## 8. Source-Ready Correction

Six imported source rows were inspected against source/audit ledgers:

- `v080149_cat02_practice_family_cat02_pf_066`
- `v080149_cat02_practice_family_cat02_pf_070`
- `v080149_cat03_contrast_cat03_cs_010`
- `v080149_cat03_practice_family_cat03_pf_041`
- `v080149_cat03_practice_family_cat03_pf_044`
- `v080149_cat08_practice_family_cat08_pf_052`

Five rows had letter-only or bare-answer visible correct choices and were corrected in place. The CAT03 contrast row was already rendered correctly. IDs and `sourceRowAuditKey` values were preserved.

No duplicate replacement rows were created.

## 9. Landing Hub Cleanup

Home now uses grouped route collections:

- Learn
- Review
- Progress
- Production
- Tools
- Settings

Old scattered learner route tiles were moved under these groups. Developer/QA access remains available under Settings as `Developer / QA`.

The static gate confirmed six top-level route groups and no forbidden Home terms:

- `Evidence Coverage`
- `Target Family`
- `Task-Type Breadth`
- `activeB1Evidence`

## 10. B1 Progress Handling

B1 Progress remains visible but compact on Home. The full detail remains in Learning Map. Wording was adjusted away from `% stars`, `practice points`, `Satzbau`, and `Repair` where the patch scope allowed.

Star semantics and medal/rank visuals were not changed.

## 11. Review Tests / Review Edit List

Review Tests remain in Dev Tools and now use `b1DevReviewTestEligible`.

The gate confirms:

- Review Tests wiring remains present.
- Review Edit List wiring remains present.
- tab-delimited copy header remains present.
- excluded bank-generated rows remain available for Review Tests.
- CAT18 remains excluded from Review Tests by default.

## 12. CAT18 Preview-Only Result

CAT18 rows remain present as source/debug material and are still:

- `previewOnly:true`
- `activeB1Evidence:false`
- not normal learner eligible
- not Review Tests eligible by default

## 13. Storage / Export / Import

Protected keys unchanged:

- `ozmosis_b1_sprint_progress_v1`
- `ozmosis_b1_progress_evidence_v1`

Additive compatible keys unchanged:

- `ozmosis_b1_path_unlock_overrides_v1`
- `ozmosis_b1_review_edit_queue_v1`

Export/import code still includes B1 progress, evidence, B1 Path unlocks, and Review Edit List payloads. No destructive migration was added.

## 14. Static Gate Result

Ran:

`_development-files/v0801410_route_filtering_landing_cleanup_gate.cjs`

Result: passed.

Key output:

- Imported rows: `6609`
- Normal imported learner rows: `1313`
- Normal imported rows by source:
  - `contrast_sets`: `253`
  - `practice_families`: `1060`
- Review Tests rows: `4951`
- Preview/CAT18 rows: `345`
- Home top cards: `6`
- Warnings: `0`

## 15. Existing Import Gate Result

Ran before version bump:

`_development-files/v080149_full_b1_v02_content_import_gate.cjs`

Result: passed.

The older gate is intentionally pinned to v0.80.14.9, so it was run before changing the app/export version. Import counts remained unchanged after the source-ready choice corrections.

## 16. Browser Smoke Result

Created:

`_development-files/v0801410_route_filtering_landing_cleanup_browser_smoke.cjs`

Attempted to run it with local headless Microsoft Edge / Chrome DevTools Protocol.

Result: blocked, not passed.

Observed blocker:

- Edge started and exposed a DevTools target.
- The harness timed out on `Page.enable`.
- A focused CDP transport debug showed Edge accepted the WebSocket handshake but reset the connection after CDP commands.
- The Browser plugin in-app runtime was also attempted and failed during setup with a Windows sandbox spawn error.

No browser, responsive screenshot, live route smoke, or Arabic/LTR browser pass is claimed for this patch.

## 17. Syntax Checks

Passed:

- `index.html` inline scripts parsed: `22`
- `content/b1-connectors.js`
- `content/b1-redemittel.js`
- `content/b1-wortschatz.js`
- `content/b1-m01-variant-families.js`
- `content/b1-m03-job-search-offices.js`
- `content/b1-m08-orders-delivery.js`
- new v0.80.14.10 gate script parsed
- new v0.80.14.10 browser smoke script parsed

## 18. Normal Route Sample Inspection

Static route-filtering inspection confirmed the source-backed corrected rows are normal-eligible and no longer have letter-only visible correct choices.

Browser route sample was not completed because browser validation was blocked.

## 19. Arabic / LTR

Code paths preserving German `dir="ltr"` were not changed.

Arabic browser smoke was planned in the browser harness but not completed because browser validation was blocked.

## 20. Responsive / Visual

Home grouping and overflow checks are included in the browser smoke harness, but the harness could not complete in this environment.

Manual code/markup inspection confirmed grouped Home structure and reduced top-level route groups. No live screenshot pass is claimed.

## 21. Tests Run

- Code inspection.
- Version/export inspection.
- Protected storage key inspection.
- Existing v0.80.14.9 content import gate before version bump.
- Inline script syntax parse.
- Content script syntax checks.
- New v0.80.14.10 static route-filtering/landing gate.
- Browser smoke harness parse.
- CDP availability/debug attempts.
- Browser plugin setup attempt.

## 22. Tests Not Run / Blocked

- Completed browser route smoke: blocked by local Edge CDP command timeout/reset.
- Completed in-app Browser validation: blocked by Browser plugin runtime setup failure.
- Responsive screenshots/contact sheet: blocked by browser validation.
- Live route sample in browser: blocked by browser validation.
- Arabic UI browser smoke: blocked by browser validation.
- Live export/import UI click-through: blocked by browser validation; static export/import code inspection passed.

## 23. Known Risks

- Browser validation should be rerun in an environment where Edge/CDP or the in-app Browser plugin can execute commands.
- Generic bank-generated rows remain imported for QA; they are not repaired in this patch.
- Review Tests candidate count is larger than v0.80.14.9 because bank-generated rows are intentionally preserved there for QA review.
- Older stored evidence may still contain legacy/import records, but learner-facing summaries now filter them conservatively at read time.

## 24. Final Status

`READY_FOR_REVIEW`

Reason: implementation and static validation are complete, route filtering gate passes with no warnings, import counts are preserved, and browser validation is honestly documented as blocked rather than claimed.

## 25. Recommended Next Patch

`v0.81 - Cases and Articles Completion Pass`
