# Ozmosis v0.81.1 B1-01 Extension Survival Audit

Audit status: `SURVIVED_WITH_METADATA_GAPS`

This audit was run after the v0.81 Cases & Articles acceptance gate passed. It did not change B1-01 runtime content.

## 1. Sources Inspected

- `content/b1-m01-variant-families.js`
- `index.html`
- `docs/OZMOSIS_SOURCE_BRIEF_CURRENT.md`
- `docs/OZMOSIS_DEVELOPMENT_ROADMAP_CURRENT.md`
- `docs/OZMOSIS_FEATURE_REGISTER_CURRENT.md`
- `docs/qa/v0.81_cases_articles_completion.md`

No B1-01 extension ZIP/package source was present in the current repo. The audit therefore uses the current runtime content file as source of truth.

## 2. Current B1-01 Runtime Shape

- `window.OZMOSIS_CONTENT.b1M01VariantFamilies`: 0 items.
- `window.OZMOSIS_CONTENT.b1M01SingleCanonical`: 259 items.
- Total current B1-01 runtime items: 259.
- Content import version: `v0.80.14.9`.
- Current category ID: `CAT02`.
- Current module key: `b1-01`.
- Current content bank key: `b1_m01`.

The earlier extension work survived as a canonical imported item array, not as populated variant-family source rows.

## 3. Baseline Families

Original B1-01 baseline family metadata is present through:

- `targetFamilyId`;
- `targetId`;
- `variantFamilyId`;
- `siblingVariantGroup`;
- `reviewGroup`;
- `familyType`;
- `familyTarget`.

No item was missing `targetId`, `variantFamilyId`, or `reviewGroup`.

## 4. Approved Extension Families

The current repo does not include the original approval inventory or extension package. From runtime evidence only, approved imported rows are present as `reviewStatus: imported` or `reviewStatus: review_flagged_imported`.

Count:

- `imported`: 231.
- `review_flagged_imported`: 28.

This is enough to prove the imported B1-01 bank survived, but not enough to prove every earlier approval/cut decision without the original source package.

## 5. Cut Families

Cut-family source manifests were not present in the repo. This audit cannot prove every cut family remained absent.

No duplicate runtime IDs were found, and no orphaned runtime rows were found.

## 6. ID Stability

Static audit result:

- duplicate item IDs: 0.
- duplicate target IDs: 0.
- missing item IDs: 0.
- missing `targetId`: 0.
- missing `variantFamilyId`: 0.
- missing `reviewGroup`: 0.

Runtime IDs therefore appear stable in the current file.

## 7. Task Depths and Item Types

Task-depth counts:

- `contrast`: 135.
- `recognition`: 51.
- `cloze`: 47.
- `sentence_building`: 8.
- `repair`: 10.
- `controlled_production`: 8.

Item-type counts:

- `multiple_choice`: 136.
- `phrase_choice`: 50.
- `vocab_cloze`: 39.
- `redemittel_cloze`: 8.
- `word_order_cloze`: 8.
- `error_repair`: 10.
- `production_card`: 8.

There are no transformation-like rows flagged by task-depth/task-type search in the current runtime file.

## 8. Routeability

B1-01 is present in the runtime registry as:

- key: `b1_m01`;
- label: `B1-01 Getting Started`;
- source: `content/b1-m01-variant-families.js`;
- item count: 259.

The runtime exposes `window.ozmosisStartB1M01(filter)`. This indicates B1-01 is routeable through the current app architecture, even though it is not the next active implementation patch.

## 9. Learning Map / Skill Matrix Visibility

B1-01 items have:

- `contentBankKey: b1_m01`;
- `moduleKey: b1-01`;
- `activeB1Evidence: true`;
- task-depth metadata;
- skill/tag metadata.

This is enough for the current Learning Map / Skill Matrix evidence pipeline to classify records if B1-01 items are practised.

## 10. Metadata Gaps

Gaps and risks found:

- 45 items are missing `supportPresentation`.
- 78 items have non-empty issue codes or non-`none` severity.
- 28 items are `review_flagged_imported`.
- 28 items have `needsHumanReview: true`.
- 28 items have `reviewFlag: true`.
- 8 production cards are self-marked production and must not be treated as correctness evidence.
- 6 items use `practice_only` evidence.

These gaps are not fixed in this patch because the prompt forbids B1-01 content rewriting.

## 11. Review-Only / Inactive Rows

All 259 rows have `status: active`.

However, 28 active rows are review-flagged imported rows. They should be treated as audit targets before any future B1-01 completion claim. This does not block v0.82 Prepositions, but it blocks saying B1-01 is clean or complete.

## 12. Encoding Check

Static UTF-8 loading through Node showed German text decoded correctly. The terminal may display some older rows with mojibake depending on console encoding, but the file itself loaded with normal Unicode strings in the audit script.

## 13. Audit Classification

`SURVIVED_WITH_METADATA_GAPS`

Reason:

- Runtime B1-01 content survived.
- IDs and grouping fields are stable.
- No duplicates or orphaned runtime rows were found.
- Routeability is present.
- Metadata gaps and review-flagged rows remain.
- Original extension approval/cut packages are not present, so exact source-lineage survival cannot be fully proven from the repo alone.

## 14. Recommended Follow-Up

Do not re-import B1-01 blindly.

Before a B1-01 completion or cleanup patch, create a narrow B1-01 metadata repair prompt covering:

- missing `supportPresentation`;
- review-flagged imported rows;
- issue-code triage;
- production/self-marked evidence labels;
- whether any rows should move to review-only/inactive.

This is non-blocking for `v0.82 - Prepositions Completion Pass`.
