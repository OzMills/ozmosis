# v0.85.11-live Deployment Diagnosis

## 1. Local Package State

The local repo-root package is v0.85.11.

- App version: `v0.85.11`
- Export version: `v0.85.11-final-regression-live-readiness`
- Inline scripts parsed: 1
- Content scripts parsed: 26
- Article TSV rows parsed: 36

## 2. Repo Root State

The repo root contains `index.html`, `content/`, `data/`, `docs/`, `README.md`, `CHANGELOG-FULL-OZMOSIS.txt`, and `AGENTS.md`.

The repo-root `index.html` is the current v0.85.11 app package.

## 3. Git / Remote State, if available

`git` is not available as an executable in this environment, so no push/deploy action was attempted.

The local `.git/config` file shows:

- Remote: `https://github.com/OzMills/ozmosis.git`
- Branch: `main`
- Stored local main ref: `f3e1c0ca3ef827f5205c7190e4e88bf8354b9093`
- Stored origin/main ref: `f3e1c0ca3ef827f5205c7190e4e88bf8354b9093`

## 4. GitHub Pages Source Clues

No local `.github/workflows`, `CNAME`, `.nojekyll`, or docs-source Pages clue was found.

Repo docs say the deployed app remains repo-root `index.html`.

Because the remote repository is `OzMills/ozmosis`, the expected GitHub project Pages path is:

`https://ozmills.github.io/ozmosis/`

## 5. Live URL Result

Formal requested URL:

`https://ozmills.github.io/kasuskonsole/`

Result: HTTP 404.

Repo-name URL checked for diagnosis:

`https://ozmills.github.io/ozmosis/`

Result: HTTP 200, title `Ozmosis`, detected app version `v0.85.11`, detected export version `v0.85.11-final-regression-live-readiness`.

## 6. Stale/404 Diagnosis

This is not a stale older build at the repo-name Pages URL. The current package is live at `/ozmosis/`.

The requested legacy path `/kasuskonsole/` is wrong or no longer backed by a Pages project that serves the current app.

## 7. Exact Required Deployment Action

No deployment action is required for `https://ozmills.github.io/ozmosis/`; it already serves v0.85.11.

If the required public URL must remain `https://ozmills.github.io/kasuskonsole/`, one of these actions is required outside Codex:

1. Restore or configure a GitHub Pages project at `OzMills/kasuskonsole` and publish the current v0.85.11 package there.
2. Rename or otherwise map the repository so the Pages project path is `/kasuskonsole/`.
3. Officially update project documentation and user-facing links to use `https://ozmills.github.io/ozmosis/` as the canonical live URL.

## 8. Whether Codex Can Safely Perform It

Codex cannot safely perform deployment here:

- `git` is unavailable.
- GitHub Pages settings cannot be inspected or changed from this local environment.
- The required action is a repository/path/settings decision, not an app runtime fix.

## 9. Whether v0.86 Is Unlocked

No.

The local package is current and v0.85.11 is live at `/ozmosis/`, but the formal requested `/kasuskonsole/` URL does not serve v0.85.11. v0.86 should remain blocked until `/ozmosis/` is explicitly accepted as the canonical live URL or `/kasuskonsole/` is configured to serve v0.85.11.

## 10. Recommended Next Patch

`v0.85.11-live-url - Canonical Live URL / Pages Path Decision`
