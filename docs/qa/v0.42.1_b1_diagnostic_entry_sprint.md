# Ozmosis v0.42.1 B1 Diagnostic Entry Sprint QA

## 1. Source Files Inspected

- `AGENTS.md`
- `README.md`
- `CHANGELOG.txt`
- `docs/OZMOSIS_SOURCE_BRIEF_v0.42.md`
- `docs/OZMOSIS_DEVELOPMENT_ROADMAP_v0.42.md`
- `docs/Kasuskonsole Source Brief.md`
- `docs/qa/v0.39_static_content_qa.md`
- `docs/qa/v0.40_progress_evidence_model_v1.md`
- `docs/qa/v0.40.1_confidence_guess_tracking.md`
- `docs/qa/v0.41_b1_skill_tag_audit.md`
- `docs/qa/v0.42_learning_map_data_scaffold.md`
- `index.html`
- `content/b1-connectors.js`
- `content/b1-redemittel.js`
- `content/b1-wortschatz.js`
- `content/ozmosis_b1_connector_sprint_v1_bank.json`
- `content/ozmosis_b1_wortschatz_survival_set_v1.json`

## 2. Starting Version Verified

Starting state was verified as v0.42:

- `README.md` said current working version `v0.42` and next planned patch `v0.42.1`.
- `AGENTS.md` said current working version `v0.42` and next planned patch `v0.42.1`.
- `CHANGELOG.txt` latest entry was `v0.42 - Versioned Docs Alignment + Learning Map Data Scaffold`.
- `docs/qa/v0.42_learning_map_data_scaffold.md` existed.
- `docs/OZMOSIS_SOURCE_BRIEF_v0.42.md` and `docs/OZMOSIS_DEVELOPMENT_ROADMAP_v0.42.md` existed.
- `index.html` contained `B1_CONTENT_REGISTRY`, `ozmosis_b1_sprint_progress_v1`, `ozmosis_b1_progress_evidence_v1`, confidence capture, and the internal Learning Map scaffold.
- Extracted static content bank files existed under `content/`.

No conflict was found that blocked v0.42.1.

## 3. v0.42 Constraints Used

- The Learning Map scaffold exists internally and is debug-only.
- B1 metadata remains mostly derived, not canonical item-level metadata.
- The diagnostic must produce starting evidence only.
- No learner-facing Learning Map UI, Skill Matrix UI, mastery claim, durability claim, transfer-ready claim, recommendation, or B1-ready claim is allowed.
- Today's B1 Sprint composition and connector inclusion policy must remain unchanged.

## 4. Files Changed

- `index.html`
- `README.md`
- `AGENTS.md`
- `CHANGELOG.txt`
- `docs/OZMOSIS_SOURCE_BRIEF_v0.42.md`
- `docs/OZMOSIS_DEVELOPMENT_ROADMAP_v0.42.md`
- `docs/qa/v0.42.1_b1_diagnostic_entry_sprint.md`

Content bank files were not modified.

## 5. Diagnostic Route / Home Card

The Home hub now includes an optional route card:

- Label: `B1 Diagnostic Sprint`
- Subtitle: `Find your starting points`
- Meta: `18 items · about 8 min · START`
- Status pill: `NEW`

The diagnostic is not forced on first launch. It starts only when the Home card is selected.

## 6. Diagnostic Queue Shape

The diagnostic queue builder uses existing B1 banks through `B1_CONTENT_REGISTRY`.

- Total items: 18
- Duplicate item IDs: none found in browser selection tests
- Production cards: at most 1
- Banks used: existing `core`, `connectors`, `redemittel`, and `wortschatz`
- No new content bank was added
- No item content, IDs, statuses, or item types were changed

The executed browser selection test produced a valid 18-item queue with all four existing B1 banks represented, no duplicate item IDs, and exactly one production card.

## 7. Diagnostic Category Coverage

| Category | Intended count | Actual test result | Source banks | Fallback limitations |
| --- | ---: | ---: | --- | --- |
| cases/articles | 3 | 3 | core | 2 fallback items were used because metadata is derived and not enough canonical case/article tags exist. |
| prepositions | 2 | 2 | core plus nearest safe B1 fallback where needed | Metadata is derived from prompt/domain text and fallback rules. |
| word_order | 2 | 2 | connectors | Selected from connector `word_order_cloze` / sentence-building items. |
| connectors | 3 | 3 | connectors | Covered connector choice and connector sentence-control items. |
| redemittel | 3 | 3 | redemittel | Covered Redemittel cloze/choice/repair-capable items. |
| wortschatz | 3 | 3 | wortschatz | Covered Wortschatz cloze/choice/repair-capable items. |
| repair | 1 | 1 | existing B1 banks | Selected from available `error_repair` items. |
| production_readiness | 1 | 1 | redemittel or wortschatz | Exactly one existing `production_card` was included. |

The diagnostic categories should be treated as conservative starting buckets because v0.41 found that metadata is mostly derived.

## 8. Diagnostic Evidence Behaviour

- Auto-graded diagnostic answers append evidence to `ozmosis_b1_progress_evidence_v1`.
- Diagnostic records use `attemptContext: "diagnostic"`.
- Correctness is preserved for auto-graded answers.
- Production-card diagnostic evidence uses the existing production-card semantics with `correct: null`.
- Confidence selection reuses the existing v0.40.1 confidence row and stores `sure`, `unsure`, `guessed`, or `null`.
- Selecting confidence updates the existing evidence record and does not append a duplicate evidence record.

## 9. Learning Map Scaffold Impact

Diagnostic evidence contributes to the existing internal scaffold exposed through:

```js
window.ozmosisB1Debug().learningMap
```

Headless browser validation confirmed that diagnostic evidence:

- increased relevant node `evidenceCount` values;
- updated `confidenceCounts`;
- updated `taskDepthCounts`;
- updated `bankCounts`;
- remained recorded as derived metadata unless explicit metadata was genuinely present;
- stayed identifiable through `attemptContext: "diagnostic"`.

No learner-facing Learning Map UI was added.

## 10. Result Screen Behaviour

The diagnostic result screen uses:

- Heading: `Diagnostic complete`
- Supporting line: `This is a starting snapshot, not a test score.`

The result summary shows:

- items attempted;
- correct count;
- unsure / guessed count;
- up to three early weak signal areas;
- up to three areas where not enough evidence remains.

The result screen does not use pass/fail, mastery, B1-ready, durable, reliable, or transfer-ready language.

## 11. Export / Import Impact

- Export still runs.
- New export metadata uses v0.42.1 metadata.
- Export includes diagnostic evidence because evidence is exported through the existing `evidence` field.
- Import UI remains available.
- Import code was not changed.
- A full export/import round-trip was not completed in this pass; see Tests Not Run.

## 12. LocalStorage Impact

Storage keys were preserved:

- `ozmosis_b1_sprint_progress_v1`
- `ozmosis_b1_progress_evidence_v1`

No diagnostic-specific persistent aggregate storage key was added. Diagnostic summaries are derived from existing runtime/evidence data.

## 13. Learner-Facing UI Impact

- Added the optional B1 Diagnostic Sprint Home card.
- Added the diagnostic route/result summary.
- Did not add a learner-facing Learning Map UI.
- Did not change existing B1 practice routes.
- Did not change Today's B1 Sprint composition.

## 14. Responsive Result

Responsive headless browser checks were run at:

- 320x568
- 390x844
- 430x932
- 768x1024
- 1365x599
- 1440x900

The Home diagnostic card, diagnostic prompt/action area, confidence row, Continue/Next action, and diagnostic result screen were reachable. No horizontal overflow was detected in the checked viewports.

## 15. Arabic / LTR Result

Arabic/LTR smoke was run in headless browser tooling. German diagnostic prompt/input content remained LTR. The Arabic UI direction behaviour was not treated as a full RTL regression because the checked diagnostic route reported LTR document direction in this session.

## 16. Live GitHub Pages Result If Run

Live GitHub Pages was not tested in this pass. Prior reports indicated a live/repo mismatch, so deployment status remains a deferred v0.42.2 finding unless separately retested.

## 17. Real iOS Safari Result

Real iOS Safari was not tested. Manual iPhone Safari keyboard testing is recommended because v0.42.1 adds a new visible diagnostic flow with answer inputs and confidence controls.

## 18. Tests Run

Code inspection:

- Located Home route/hub rendering.
- Located Today's B1 Sprint route and confirmed its composition path was not changed.
- Located `B1_CONTENT_REGISTRY`.
- Located B1 round construction and existing evidence helpers.
- Located v0.40.1 confidence update logic.
- Located v0.42 Learning Map scaffold/debug access.
- Confirmed content banks were not edited.

Diagnostic selection test:

- Started B1 Diagnostic Sprint from Home.
- Confirmed 18 total items.
- Confirmed no duplicate item IDs.
- Confirmed at most one production card.
- Confirmed items came from existing B1 banks.
- Confirmed category quotas were represented, with derived-metadata fallbacks where needed.

Evidence recording test:

- Answered one auto-graded diagnostic item correctly and confirmed one `diagnostic` evidence record.
- Answered one auto-graded diagnostic item incorrectly and confirmed one additional `diagnostic` evidence record.
- Selected `sure`, `unsure`, and `guessed` across diagnostic items and confirmed confidence updated existing records.
- Marked the diagnostic production card practised and confirmed `attemptContext: "diagnostic"` with production-card semantics.
- Confirmed evidence survived reload.

Learning Map scaffold test:

- Inspected scaffold before and after diagnostic evidence.
- Confirmed node evidence, confidence, task-depth, bank, and provenance summaries updated.
- Confirmed diagnostic evidence remained distinguishable by attempt context.

Syntax check:

```powershell
$node='C:\Users\rossm\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'; $js = @'
const fs = require("fs");
const vm = require("vm");
const html = fs.readFileSync("index.html", "utf8");
const scripts = [...html.matchAll(/<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/gi)].map((m) => m[1]);
scripts.forEach((code, i) => new Function(code));
["content/b1-connectors.js", "content/b1-redemittel.js", "content/b1-wortschatz.js"].forEach((f) => new vm.Script(fs.readFileSync(f, "utf8"), { filename: f }));
console.log("parsed " + scripts.length + " inline scripts and 3 content scripts");
'@; $js | & $node -
```

Result: `parsed 1 inline scripts and 3 content scripts`.

Browser smoke:

- Loaded the app locally through `http://127.0.0.1:8765/index.html` in headless Edge.
- Confirmed no console errors during the diagnostic, route, persistence, responsive, and result-screen smoke checks.

Route smoke:

- Home
- B1 Diagnostic Sprint
- Today's B1 Sprint
- Grammar / Connector Sprint
- Redemittel
- Wortschatz
- Case Trainer
- Options
- Dev Tools

Persistence/export smoke:

- Confirmed `ozmosis_b1_sprint_progress_v1` survived reload.
- Confirmed `ozmosis_b1_progress_evidence_v1` survived reload.
- Confirmed export still runs and includes diagnostic evidence.
- Confirmed import UI remains available.

## 19. Tests Not Run and Why

- Full export/import round-trip with diagnostic evidence: not completed because the available local browser automation path hit command/sandbox practical limits during the round-trip attempt. Export and import UI were verified, and import code was not changed.
- Live GitHub Pages: not run; deployment status is deferred.
- Real iOS Safari: not available in this environment.
- Full Arabic UI RTL regression: not run; only German diagnostic content LTR smoke was checked.

## 20. Known Limitations

- Metadata is still mostly derived, so diagnostic category labels are conservative starting buckets.
- The diagnostic is not a placement test.
- The diagnostic does not claim mastery, B1 readiness, durable knowledge, or transfer readiness.
- No learner-facing Learning Map UI exists yet.
- No Skill Matrix exists yet.
- No weakness repair exists yet.
- Connector inclusion in Today's B1 Sprint was intentionally not changed.
- Production-card useful phrase gaps were intentionally not repaired.
- Live deployment status remains separate from this repo-local patch.

## 21. Recommended Next Patch

Recommended next patch:

`v0.42.2 - Deferred Findings Recovery / Policy Decisions`

Suggested scope:

- decide connector inclusion policy for Today's B1 Sprint;
- decide whether production-card useful phrase gaps need a repair pass;
- record live GitHub Pages deployment status;
- record manual iPhone Safari result;
- decide whether diagnostic evidence is clean enough before v0.43 Learning Map UI preparation.

